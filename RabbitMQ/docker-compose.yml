version: '3.1'
services:
  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - '5672:5672'
      - '15672:15672'
    environment:
      - RABBITMQ_DAFAULT_USER=example
      - RABBITMQ_DEFAULT_PASS=example
